import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // NOTE: turbopack.root config intentionally omitted — not a valid key in
  // ExperimentalConfig for this Next.js version. The dual yarn.lock warning
  // is cosmetic only and does not affect builds.
};

export default nextConfig;
